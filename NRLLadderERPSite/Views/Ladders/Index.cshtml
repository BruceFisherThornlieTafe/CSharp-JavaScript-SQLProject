@model IEnumerable<NRLLadderERPSite.Models.Ladder>

@{
    ViewBag.Title = "Index";
}

<h2>Ladder Table Data Entry</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Position)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ladder_Dif)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Position)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ladder_Dif)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PKFK_TeamID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PKFK_TeamID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PKFK_TeamID })
        </td>
    </tr>
}

</table>
